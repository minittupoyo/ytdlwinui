# Contributing

IssueやPull Requestを歓迎します。

## 開発手順

1. リポジトリをforkして作業ブランチを作成します。
2. `YtdlWinUI` ディレクトリで変更します。
3. `dotnet build .\YtdlWinUI.csproj -p:Platform=x64` を実行します。
4. UI変更は `BuildAndRun.ps1` で起動し、ライト／ダークテーマとキーボード操作を確認します。
5. 変更理由、確認手順、UI変更時のスクリーンショットをPull Requestへ記載します。

ダウンロードしたメディア、ブラウザーCookie、ログ、ビルド成果物、認証情報はコミットしないでください。
